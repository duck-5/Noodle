version: '3.8'

services:
  tautracker:
    build: .
    container_name: tautracker-app
    ports:
      - "8000:8000"
    volumes:
      - ./db:/app/db
    env_file:
      - .env
    restart: unless-stopped
